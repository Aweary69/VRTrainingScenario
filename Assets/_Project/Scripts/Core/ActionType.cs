namespace Project.Core
{
    /// <summary>
    /// Типы действий, которые игрок может выполнить в сценарии.
    /// Используется как часть ключа для поиска ожидаемого действия (см. ActionKey).
    /// Список соответствует ТЗ: "дойти до точки интереса, взять объект(граб),
    /// кликнуть на объект(поинтер/луч), нажать на кнопку UI".
    /// </summary>
    public enum ActionType
    {
        MoveToPoint,
        Grab,
        Click,
        PressButton
    }
}
